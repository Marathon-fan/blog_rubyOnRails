<h1>Blog Posts</h1>
<% @post.each do |post| %>
    <div class="well">
      <h3><%= post.title %></h3>
      <p><%= post.body %></p>
      <%= link_to "Read More", {:controller=>"posts", :action=> "show", :post_id=>post.id}, :class => 'btn btn-default' %>
    </div>
<% end %>
